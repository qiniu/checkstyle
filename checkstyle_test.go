package checkstyle

import (
	// "io/ioutil"
	"testing"
)

func TestFileLine(t *testing.T) {
	// baseDir := "testdata/"
	// file, err := ioutil.ReadFile(baseDir + "fileline.go")
	// _checkerOk := checker{FileLine: 50}
	// ps, err := _checkerOk.Check("fileline.go", file)
	//    if err !=  {

	//    }
}
